{
  "name": "paginawebviajes",
  "version": "0.0.1",
  "description": "pagina viajes",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "export NODE_ENV=production || NODE_ENV=production  || SET \"NODE_ENV=production\"  && node server/",
    "dev": "export NODE_ENV=development || NODE_ENV=development  || SET \"NODE_ENV=development\"  && nodemon server/"
  },
  "author": "Javi Blanco",
  "license": "MIT",
  "dependencies": {
    "bcrypt": "^3.0.5",
    "express": "^4.16.4",
    "lodash": "^4.17.11",
    "mongoose": "^5.4.21",
    "multer": "^1.4.1"
  },
  "devDependencies": {
    "nodemon": "^1.18.10"
  }
}
